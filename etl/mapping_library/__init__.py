"""Standard chart-of-accounts mapping library (reporting-v2 Phase 4).

Holds the exported standard mapping (``finssentials_standard_v1.json``) that drives
the auto-suggest path for new clients (see ``etl/mapping_suggest.py``), plus the
generator (``export_standard.py``) that produces it from a live/clone DB.
"""
